type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - id
      - type
    properties:
      type:
        type: string
        enum: [countries]
      id:
        type: string
        description: "ISO 3166-1 alpha-3"
        pattern: "^[A-Z]{3}$"
