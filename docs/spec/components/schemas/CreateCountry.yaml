type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ create_country ]
      attributes:
        type: object
        required:
          - name
        properties:
          name:
            type: string
            description: "city name"
